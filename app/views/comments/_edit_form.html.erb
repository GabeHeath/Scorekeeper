<%= form_for [@play, @comment] do |f| %>
    <p>
      <%= f.text_area :comment, :rows => 5, :class => "form-control" %>
    </p>
    <br>
    <p>
      <%= f.submit "Submit Comment", :class => 'btn btn-primary' %>
    </p>
<% end %>